# Multiple-Linear-Regression
## Example

```r
if (!require(readxl)) install.packages("readxl")
if (!require(car)) install.packages("car")
if (!require(ggplot2)) install.packages("ggplot2")
if (!require(plotly)) install.packages("plotly")

# Load necessary libraries
library(readxl)
library(ggplot2)
library(car) # For VIF calculation
library(plotly) # For 3D visualization

2. Simpan file README Anda.

---

### Penjelasan
- **\`\`\`r**: Menandai bahwa kode dalam blok tersebut adalah kode R. Ini akan memberikan pewarnaan sintaks R di GitHub.
- **Kode R**: Masukkan kode Anda di antara tanda pembuka (\`\`\`r) dan penutup (\`\`\`).
- **\`\`\`**: Menutup blok kode.

Hasilnya akan tampak seperti blok kode R dengan pewarnaan sintaks di README GitHub saat dilihat pada repositori.
